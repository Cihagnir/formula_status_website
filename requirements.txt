
# Basic Requirements and Their Dependencies for the Backend

# I am using an AWS EC2 instance as a backend server. I will briefly explain how you can set up your environment.

# ==== Create Our Virtual Environment ==== 
# `python3 -m venv "your_venv_name"` -> To create a virtual environment.
# Due to EC2 security rules, we have to work within the virtual environment we create.
# ======== 

# ==== Install the Requirements ==== 
# `./venv/bin/pip3 install -r requirements.txt`
# ======== 

# You are ready to go, baby!

# ==== Run the Backend ==== 
# `./venv/bin/python3 main.py`
# ========


blinker==1.9.0
click==8.1.8
Flask==3.1.0
Flask-MySQL==1.6.0
itsdangerous==2.2.0
Jinja2==3.1.5
MarkupSafe==3.0.2
numpy==2.2.1
packaging==24.2
pandas==2.2.3
plotly==5.24.1
PyMySQL==1.1.1
python-dateutil==2.9.0.post0
pytz==2024.2
six==1.17.0
tenacity==9.0.0
tzdata==2024.2
Werkzeug==3.1.3
